# Frontend — Remaining Work

Replaces `CLAUDE_CODE_PROMPTS.md`. Tasks 1–14 are done and their prompts are deleted; only live work remains.

**Done:** Phase A + Phase B feature-complete on MSW (search → filters → trip details → seat map with gender → lock → checkout + countdown → booking + ticket).
**Conventions:** `CLAUDE.md`. **Contract:** `docs/BACKEND_V1.md`. **QA:** `docs/QA_PHASE_B_MANUAL.md`.

## Order

```
1. Manual QA round (QA_PHASE_B_MANUAL.md, 52 cases)
2. Task 15 — hardening (consumes the QA findings)
3. E1 + E2   ← after backend M2
4. E3        ← after backend M4
5. E4        ← tickets + PNR lookup
6. E5        ← optional, post-launch
```

Task 15 must land **before** Phase E: enter the cutover with UX you trust, so every failure is a shape mismatch.

---

## Task 15 — Phase B hardening

Paste the failed QA cases into PART 1 before running.

```
Hardening pass over the booking flow. No new features, no new routes, no new dependencies.

=== PART 1 — QA findings (fix these first) ===
<<<PASTE failed QA case IDs with expected vs actual. If none, write "none".>>>

=== PART 2 — Known weak points ===
1. The 400ms mount-duration guard in the release-on-leave effect is a heuristic.
   Replace it: useEffect cleanup as the primary path + a "pagehide" listener instead of
   "beforeunload" (beforeunload is unreliable on iOS Safari). Keep a StrictMode-safe pattern
   (a ref flag set after the first real commit), but drop the time-based check.
2. The Date.now() snapshot in trip-detail: confirm the CTA state is still correct if the page stays
   open past departure time. A one-minute interval is acceptable if cheap; otherwise document why
   the snapshot is fine.

=== PART 3 — Audits (report counts, then fix) ===
3. grep src/ for "response?.status", "err.status", "status ===", "statusCode" → must be ZERO hits.
   All error UX keys on ApiError.code. Fix any hit.
4. Every mutation and query has an error path; any unhandled ApiError.code falls through to
   toast "حدث خطأ، حاول مجدداً". Verify: seat map fetch, lock, release, createBooking, trip fetch.
5. Loading/disabled audit: seat map skeleton, lock button pending, checkout submit pending,
   confirmation while the store hydrates. No layout shift on data arrival.
6. RTL audit of Tasks 10–14 screens: no pl-/pr-/ml-/mr- outside src/components/ui; chevron/arrow
   direction; dialog button order.
7. Gender icon + color identical across seat map, selection bar, checkout badge, ticket table.
   Single source = gender-icon.tsx. Fix any divergence.
8. All user-facing strings from src/messages/ar.json. grep for hardcoded Arabic literals in
   src/features/booking and src/app/booking — report and move them.

NOT in this task: supabase, new components, redesign, unrelated refactors.

Verify: npx tsc --noEmit && npm run lint && npm run build, then prettier.
Report anything found but deliberately not fixed, with the reason.
```

---

## Task E1 — Supabase client (after backend M2)

```
1. src/lib/supabase.ts: browser client from src/config/env.ts. This file and features/*/api.ts are
   the ONLY places importing @supabase/supabase-js.
2. src/lib/rpc.ts: callRpc(name, args, schema) = supabase.rpc() + reuse unwrap() from lib/envelope.ts.
   Do NOT reimplement the envelope logic — it is shared with the MSW path.
3. Types already live at src/types/database.ts (generated by `npm run db:types` in this repo).
   Confirm they are fresh: run db:types and verify git diff is empty.
No feature swapped yet.
Verify: npm run build, plus one manual callRpc against the dev project from a scratch page.
```

## Task E2 — Swap catalog + search

```
Rewrite src/features/search/api.ts internals ONLY:
  getCities   → supabase.from("cities").select()
  searchTrips → callRpc("search_trips", { from_slug, to_slug, travel_date, passengers }, schema)
  getTrip     → callRpc("get_trip", { trip_id }, schema)
Signatures, zod schemas, hooks, and components unchanged. Remove the cities/search MSW handlers.
Verify /search and /trips/[id] against the dev seed. Report any field-shape mismatch as a
BACKEND_V1 doc issue — do not silently adapt the frontend schema.
```

## Task E3 — Swap booking (after backend M4)

```
Rewrite src/features/booking/api.ts:
  lockSeats     → callRpc("lock_seats", { p_trip_id, p_seats })
  releaseLock   → callRpc("release_lock", { p_lock_id })
  createBooking → callRpc("create_booking", { p_lock_id, p_idempotency_key, p_payment_method, p_passengers })
  cancelBooking → callRpc("cancel_booking", { p_booking_id })
Ensure an anonymous session exists before the first lock (supabase.auth.signInAnonymously() if none).
Remove the booking MSW handlers.
Verify on dev: happy path · real concurrent conflict (two browsers, same seat) · LOCK_EXPIRED via the
QA script · double-tap idempotency · gender visible cross-browser on locked seats.
These are the QA cases MSW could not cover — run them here for the first time.
```

## Task E4 — My tickets + PNR lookup (PAS-10, PAS-11)

```
1. /tickets (رحلاتي): supabase.from("bookings") — RLS scopes to the anonymous session.
   Tabs القادمة/السابقة, newest first, paginated. Empty state explains tickets are per-device and
   links to /tickets/lookup.
2. /tickets/[id]: full ticket with QR (reuse the confirmation components).
3. /tickets/lookup: PNR (6 chars, uppercased on input) + phone (+963 regex) → callRpc("lookup_booking").
   NOT_FOUND → "تأكد من رقم الحجز ورقم الهاتف" — never reveal which field was wrong.
4. Header nav "رحلاتي" links to lookup when the local list is empty. Remove any passenger auth placeholder.
```

## Task E5 — Realtime seat map (optional, post-launch)

```
Replace the 15s polling in use-seat-map.ts with a Supabase Realtime subscription on seat_lock_seats +
booking_passengers filtered by trip_id, invalidating queryKeys.trips.seats(id) on any event.
Keep polling as fallback when the channel errors. Verify: a lock in browser A appears in browser B < 2s.
```

---

## Prompting rules

1. One task per session; ≤ ~6 files, listed explicitly in the prompt.
2. Reference docs by path — the agent reads them itself. Never paste contracts into prompts.
3. Always state what NOT to build.
4. `CLAUDE.md` carries the conventions so prompts never repeat envelope/RTL/zod/gender rules.
5. End every risky task with a concrete verification step.
6. Wrong output → revert, tighten, rerun. Never patch a bad task with follow-ups in the same session.
